{
  "name": "utils-general",
  "version": "0.0.4",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncu -u && npm install && npm update && tsc",
    "commit": "git commit -am 'commit' && npm version patch && git push",
    "test": "jest"
  },
  "jest": {
    "globals": {
      "ts-jest": {
        "useESM": true
      }
    },
    "preset": "ts-jest/presets/default-esm"
  },
  "author": "sawara7",
  "license": "MIT",
  "bundleDependencies": [],
  "dependencies": {
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "@types/node": "^20.14.11",
    "@types/uuid": "^10.0.0",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0"
  }
}
